<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F5F5F5"
    android:padding="16dp">

    <!-- Distance input + Find routes button row -->
    <EditText
        android:id="@+id/input_distance"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Distance (km)"
        android:inputType="numberDecimal"
        android:padding="12dp"
        android:background="@drawable/bg_rounded_white"
        android:singleLine="true"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/button_find_routes"
        app:layout_constraintHorizontal_bias="0"
        app:layout_goneMarginEnd="8dp"
        android:layout_marginEnd="8dp"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/button_find_routes"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="FIND ROUTES"
        android:textAllCaps="true"
        android:textSize="14sp"
        app:cornerRadius="24dp"
        app:backgroundTint="@color/purple_700"
        android:textColor="@android:color/white"
        app:layout_constraintTop_toTopOf="@id/input_distance"
        app:layout_constraintBottom_toBottomOf="@id/input_distance"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Dummy view to anchor constraints (top bar) -->
    <View
        android:id="@+id/topBar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/input_distance"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="8dp"/>

    <!-- Square-ish map container with margins -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/map_fragment"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="16dp"
        app:layout_constraintTop_toBottomOf="@id/topBar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/button_complete_route"
        app:layout_constraintDimensionRatio="1:1" />

    <!-- Floating recenter button over the map -->
    <ImageButton
        android:id="@+id/button_recenter"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_margin="16dp"
        android:background="@drawable/bg_circle_white_elevated"
        android:contentDescription="@string/app_name"
        android:padding="8dp"
        android:src="@android:drawable/ic_menu_mylocation"
        app:tint="@color/purple_700"
        app:layout_constraintBottom_toBottomOf="@id/map_fragment"
        app:layout_constraintEnd_toEndOf="@id/map_fragment" />

    <!-- New: Complete route button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/button_complete_route"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="I COMPLETED THIS ROUTE"
        android:textAllCaps="true"
        android:textSize="14sp"
        android:layout_marginTop="8dp"
        app:cornerRadius="24dp"
        app:backgroundTint="@color/purple_700"
        android:textColor="@android:color/white"
        app:layout_constraintTop_toBottomOf="@id/map_fragment"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
